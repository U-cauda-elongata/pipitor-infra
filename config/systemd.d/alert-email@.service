[Unit]
Description=Sends failure notification email for %I

[Service]
Type=oneshot
ExecStart=/bin/sh -c '{ echo "Subject: [systemd] %i"; echo; systemctl status --full "%i"; } | msmtp ursus.cauda.elongata@gmail.com'
User=pipitor
